# Copy this file to .env and fill in real values before running the app with MySQL
# To use MySQL, set DB_ENGINE=mysql
DB_ENGINE=mysql
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=change_me
DB_NAME=educonnect

# If you use Docker Compose (service name 'db'), when running the app from the host use 127.0.0.1:3306
# or use 'db' as the host inside a containerized app

# Flask secret
SECRET_KEY=dev-secret-key-change-in-production

# Optional: override SQLite file name when using SQLite fallback
SQLITE_DB=educonnect.db
# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=educonnect

# Flask Configuration
SECRET_KEY=your-secret-key-here-change-in-production
